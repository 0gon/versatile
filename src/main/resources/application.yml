server:
  datacenter-id: 1
  worker-id: 1

spring:
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: false

  thymeleaf:
    cache: false

  application:
    name: gon

  output:
    ansi:
      enabled: ALWAYS

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update # create, create-drop, update, validate, none

  h2:
    console:
      enabled: true

  datasource:
    url: jdbc:h2:tcp://localhost/D:/versatile/db/H2/data;
    #        url: jdbc:h2:tcp://localhost/C:\Dev\Project\versatile/db/H2/data;
    driver-class-name: org.h2.Driver
    username: sa
    password:

jwt:
  expiration_time: 10000 #86400000 #1일
  secret: VlwEyVBsYt9V7zq57TejMnVUyzblYcfPQye08f7MGVA9XkHa


logging:
  level:
    org:
      springframework:
#                security: DEBUG